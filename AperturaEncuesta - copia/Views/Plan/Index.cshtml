@model IEnumerable<EncuestaApertura.Models.Plan>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PlanDescripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PlanDescripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPlan }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPlan }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPlan })
        </td>
    </tr>
}

</table>
